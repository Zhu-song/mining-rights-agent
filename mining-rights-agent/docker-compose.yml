version: '3.8'

services:
  mining-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mining-rights-agent
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    command: ["给我生成一份关于 Pilbara 锂矿的今日简报"]